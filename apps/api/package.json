{
  "name": "@oh-your-ear/api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "go build -o bin/server ./cmd/server",
    "lint": "gofmt -l .",
    "typecheck": "go vet ./...",
    "clean": "rm -rf bin"
  }
}
