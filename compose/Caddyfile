{$DOMAIN} {
	tls {$ACME_EMAIL}

	# The API host. The front ends are on Vercel and reach this through the rewrite in
	# apps/web/vercel.json, so this is the only certificate that has to exist here.
	reverse_proxy api:8080

	encode zstd gzip
}
